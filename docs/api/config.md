# Config

{mod}`vcspull.config` finds, loads, and filters vcspull configuration from
Python — the same functions the CLI uses to locate configuration files,
expand workspace roots, and extract repository entries. See
{ref}`configuration` for the file format itself.

```{eval-rst}
.. automodule:: vcspull.config
   :members:
   :show-inheritance:
   :undoc-members:
```
